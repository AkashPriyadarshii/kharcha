# Kharcha — Handoff Notes

> For when one dev hands work to the other, or the AI agent switches context. Append, don't rewrite.

## How to pick up work

1. Read `CLAUDE.md` (rules), `docs/state.md` (where we are), `docs/implementation-plan.md` (what's next).
2. Pick the next unstarted step. Branch `feat/<name>`.
3. Build to CLAUDE.md standards. Verify: analyze + test + device smoke.
4. PR, get the owner's review, merge.
5. Update `docs/state.md`.

## Current handoff (as of 2026-08-09)

**To: whoever picks up next.** v0.2.2 live-bug fixes are in. **Critical: UPI capture was dead since v0.1.1** — Kotlin wrote `cacheDir/upi_inbox.jsonl`, Dart read `getApplicationCacheDirectory()` (`code_cache/`) — different dirs, inbox never drained. Dart now reads `getTemporaryDirectory()` (= getCacheDir). Capture now also drains every 30s (real-time while open). Feature deletes now tombstone-sync (schema v11 `deleted_features`); app lock fixed via `FlutterFragmentActivity`; profile label "Source" not "Open source" (LICENSE is all-rights-reserved). Next: on-device verify capture + lock + push, apply Supabase migrations 0002-0004 if not applied. Full context in `docs/state.md`.

## Known gotchas

- Supabase + Google OAuth needs a Supabase project + OAuth client configured. Create in Supabase console before Step 1.2. Owner holds console access.
- NotificationListenerService is Kotlin — requires Android project edit + platform channel. Not pure Dart.
- minSdk 32 enforced — check emulator/device is Android 12+.
- Attribution disabled — do not add Co-Authored-By trailers to commits.

## Log

- **2026-08-09** — **v0.2.6 — dark theme + bug reporting.** (1) **Dark theme** — `kharchaTheme(brightness:)` + `kharchaDarkTheme()`; light keeps warm paper `#FBF7EF`, dark uses warm near-black `#14120D`; hardcoded whites in theme.dart flipped to scheme containers (`surfaceContainer`/`surfaceContainerHigh`) so dark reads correctly. In-app semantics colors (income green / expense red / budget amber) are theme-independent — no changes needed. `ThemeMode` persisted in `theme_mode.json` (same JSON-store pattern as `AppLockStore`), loaded at startup in `main.dart`, wired as `darkTheme` + `themeMode` on MaterialApp; Profile → Settings → **Theme** tile → System/Light/Dark SimpleDialog (`RadioGroup`, non-deprecated API). (2) **In-app bug reporting** — Profile → Settings → **Report a bug**: dialog offers **Email** (recommended; prefilled `mailto:akash.priyadarshi9100backup@gmail.com` with subject + device context) or **In-app** (text dialog → insert into `bug_reports` Supabase table). **⚠ Migration 0005 must be applied to the remote project** (`supabase db push` or SQL Editor) — RLS insert policy only, `user_id = auth.uid()`, no select policy (owner reads via dashboard/service-role). Reproducer: `flutter analyze` clean; theme + bug_reporter + notification tests pass (15). APK v0.2.6 split-per-abi built. (Akash)
- **2026-08-09** — **v0.2.2 finalize pass.** (1) **Terms bug fixed at root** — `/terms` was in the go_router redirect's auth guard, so signed-out users (auth screen) were bounced to `/auth` the instant the route pushed; `main.dart` now whitelists `/terms` as public (auth + terms reachable pre-login). (2) **Money precision audit** — `parseAmount` in `lib/core/money.dart` (2dp boundary rounding) now routes every amount boundary: add/quick-add expense, budget limit, wallet balance, goal target + add, debt, subscription, CSV import, and the UPI/bank parser (was raw `double.parse`; also fixed a comma-grouping bug "Rs. 2,500.00" → was returning null). `test/money_test.dart` added. Validators (budget/quick-add/add-expense) left as-is — they only gate, don't persist. (3) **Real release signing** — `keytool` generated `android/app/kharcha-release.jks` (alias `kharcha`, 10000-day RSA 2048), wired via `android/key.properties` + `signingConfigs.release` in `build.gradle.kts`. Both files gitignored. **⚠ BACK UP `kharcha-release.jks` + `key.properties` — losing them means you cannot update an installed build (old key, uninstall required).** Release falls back to debug signing only when key.properties is missing (fresh clone/CI). (4) **Profile About section** — Akash Priyadarshi + GitHub profile/repo `launchUrl` links; `url_launcher` promoted to direct dep (was transitive). (5) **Android 16 prep** — SafeArea(top:false) around wallets/objectives/subscriptions ListViews so gesture-nav bar can't clip the last item. AGP 8.11.1 / Gradle 8.14 / Kotlin 2.2.20 / Flutter 3.44.9 already satisfy 16KB-page + targetSdk. (6) **Repo hygiene** — removed tracked `android/build/reports/problems/problems-report.html` (was inflating GitHub language stats 28.6% HTML) + gitignored `/android/build/`; README "For AI agents" quick-clone block deleted. (7) **Backup/restore audit conclusion** — everything user-entered syncs (transactions, custom categories, budgets, wallets, recurring, objectives, debts). Intentionally local by design: `rules` (learned auto-categorization — rebuilds from behavior), `merchants` (presentation only), `exchange_rates` (cache), `deleted_transactions` (internal tombstone), app-lock + onboarding flags (device-local JSON — new device shouldn't auto-lock). **Supabase migrations 0002/0003/0004 must be applied in the SQL Editor** — the MCP account is a different project and can't push them; budget push needs seeded `categories` (0003) or it fails FK 23503. (Akash)
- **2026-08-08** — **Windows debug support (chore: windows debug support).** Added Windows desktop scaffold (`windows/`) with `flutter create --platforms=windows .` — **no Android files touched**. Windows-only hardening: (1) onboarding skipped on non-Android (`main.dart` — it is Android capture setup: notification access, battery exemption); (2) home AppBar capture icon hidden on non-Android (`home_shell.dart`); (3) capture/notification channel invokes already covered by try/catch → snackbar on Windows. `local_auth_windows` present in lockfile → app lock works on Windows (Windows Hello). Notifications init is try/catch'd in `_initNotifications`. Data path on Windows = manual/quick add + CSV import (capture is Android-only). **CI changed to build the Windows exe** — `.github/workflows/build-release.yml` now `flutter build windows --release` + create/overwrite `v<pubspec version>` release with the exe (replaces arm64 APK CI). Runs on `windows-latest`. **Note:** `flutter test`/full build still blocked on this Windows host by the sqlite3 native-assets collision (`docs/troubleshooting.md`) — CI is the build gate now; `flutter analyze` clean locally. Supabase sync/Google sign-in work unchanged; guest mode is the local Windows path.

- **2026-08-08** — **Windows removed (owner request) — Android-only repo.** The `windows/` platform scaffold was debug-only and has been deleted. Windows-specific code stripped: `Platform.isAndroid` guards in `main.dart` (onboarding skip) and `home_shell.dart` (capture icon) removed — the Android branch is the only branch now; `dart:io` imports dropped. Windows-only deps (`local_auth_windows`, `path_provider_windows`, etc.) leave the lockfile automatically. CI workflow deleted (owner: "i dont want ci") — releases built on demand + pushed to GitHub. Release hardening: `android/app/build.gradle.kts` now filters `abiFilters arm64-v8a` and sets `jniLibs.useLegacyPackaging = false` (page-aligned uncompressed native libs) — fixes the "package is invalid" install error. arm64 APK built split-per-abi, renamed `kharcha-armv8a-release.apk`, uploaded to the v0.2.1 release (old `app-release.apk` asset deleted). `.codegraph/` gitignored. Note: codegraph MCP died when the `.codegraph` index was deleted by the user — it is not coming back unless re-indexed. `flutter analyze` clean (2 files, +5/-12).

- **2026-08-08** — **v0.2.0 income support + permissions (feat/v0.2.0).** Cashew income port: `is_income` bool on `Transactions` + `Categories` (Drift schema v7, `onUpgrade from<7`, Supabase `0001_init.sql` adds columns idempotently + fresh-create). Income seeded categories (Salary/Bonus/Gift/Other income, `sort_order` 10+). Repository: `insertManual`/`insertImported` take `isIncome`; income skips expense-rule auto-categorize; 8 spend aggregates (`dayTotal`/`weekTotal`/`monthTotal`/`monthSpendByCategory`/`topCategories`/`monthlyTrend`/`merchantRanking`/`paymentMethodTotals`) filter `isIncome=false`; new `dayIncome`/`monthIncome`; `_merge`/`_insertFromRemote` carry income. Sync: `is_income` in `RemoteTransaction` push/pull, `localToRemoteJson`. UI: `lib/widgets/income_expense_toggle.dart` (shared expense-red/income-green `SegmentedButton`); quick-add + add-expense wired (income mode filters categories to income ones, title + save button switch to "Add income"/"Save income"); home hero income line (mint green on dark panel) + colored recent tiles; transactions tab income/expense filter dropdown + `+₹`/`−` colored amounts. Export `type` column (CSV+JSON, expense/income), importer reads `type`. Tests: income categories seeded, income insert skips categorize, month/day totals exclude income + monthIncome/dayIncome count it, import round-trip, filter income/expense, sync `is_income` push + remote overwrite, exporter type. **Permissions pass:** manifest now full set (INTERNET/ACCESS_NETWORK_STATE — release needed for Supabase, debug had it injected; USE_BIOMETRIC; POST_NOTIFICATIONS; VIBRATE; READ/WRITE_EXTERNAL_STORAGE ≤32; READ_MEDIA_IMAGES + READ_MEDIA_VISUAL_USER_SELECTED; BIND_NOTIFICATION_LISTENER_SERVICE; WAKE_LOCK; REQUEST_IGNORE_BATTERY_OPTIMIZATIONS). `MainActivity.kt` channel gains `requestNotificationPermission` (runtime dialog, API 33+) + `requestBatteryOptimizationExemption` (`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`, fallback to list). Onboarding now 3 steps with in-app asks (capture settings link, notification dialog, battery prompt); startup `requestNotificationsPermission()` removed from `notifications.dart`. `flutter analyze` clean. **Release:** arm64 release APK built on demand (CI removed). Note: `flutter test` blocked on Windows host as always.
- **2026-08-08** — **CI re-added (owner request).** `.github/workflows/build-release.yml`: on every push to main → `flutter build apk --release --target-platform android-arm64 --split-per-abi`, then create/overwrite release tagged `v<pubspec version>` with the arm64 APK (overwrite keeps the "latest" link fresh, no tag accumulation). Runner `ubuntu-latest` (7GB — max free tier). GITHUB_TOKEN needs `contents: write` (set in workflow). NOTE: overrides the 2026-08-06 "CI removed" decision — Actions reliability is back on the owner's terms. **Removed again the same day** — owner wants no CI.
- **2026-08-08** — **Audit fixes.** (1) Sync LWW tie bug — `_merge` marked equal-timestamp rows dirty:true → re-pushed every sync forever, `syncStatus` never converged. Now tie → claim remote id + clean. (2) Wallet balance was one-shot `FutureProvider` → went stale after adding an expense; now stream-derived like the other aggregates. (3) Capture inbox drain aborted on one bad insert → replayed all earlier lines as dupes; per-line try/catch now. (4) Supabase migration `create policy` not idempotent → 42710 on re-run; added `drop policy if exists` before creates.
- **2026-08-08** — **v0.1.1 expansion, phases 1-7 (feat/v0.1.1).** UPI capture fixes (dedupe-by-text 60s window, title+text amount scan, guarded handler). CSV import (`importer.dart`, upi_ref dedupe). Wallets + multi-currency (schema v3: `Wallets`, `ExchangeRates`, `walletId` on transactions; add-expense wallet dropdown; per-wallet balance). Recurring subscriptions (schema v4: `RecurringTransactions`, due list, pay-one-tap roll-forward, pause/resume). Savings objectives (schema v5: `Objectives`, progress cards). Bill splitter (`splitBillPaisa` exact-sum integer paise, one txn per person). Categories editor (add/edit/delete custom with emoji+color, delete detaches txns, builtins read-only). Tests written for each (importer 5, wallets 6, recurring 4, objectives 3, bill_splitter 3, categories 4). `flutter analyze` clean. Full `flutter test` still blocked on Windows host (sqlite3 native-assets — recoverable per `docs/troubleshooting.md` working fix; kill `dart`/`flutter_tester` + rm `build/native_assets` + `flutter clean`). **Design pass (8):** `lib/core/theme.dart` — ink-green on warm paper, `moneyStyle` (display + tabular figures), `SectionTitle` caps labels, hero month panel with count-up + amber over-budget, themed budget alert colors, transactions/bill/wallet/objective/subscription amounts unified to `moneyStyle`. **Guest mode:** "Continue as guest" on auth screen (no Supabase session — local-only, sync no-ops, pre-release not publishing yet; remove when real sign-in is the only path). **Credit/debt ledger (9):** `Debts` table (schema v6), lend/borrow + settle + delete, net-owed strip. **Pending:** v0.1.1 arm64-only release + `gh release`. New tables local-only — Supabase mirror deferred.
- **2026-08-08** — **4.3 Reports + 4.5 Profile (feat/phase4-continued).** Reports tab: category pie + monthly trend line + top merchants (fl_chart), 4th nav tab. Profile tab: account header, payment methods, export CSV/JSON UI, backup status, app lock toggle, 5th nav tab. App lock is now **real**: `AppLockStore` (JSON file, `path_provider`), `AppLockController` (Riverpod), toggle in Profile prompts OS biometric on enable; `LockGate` widget in `main.dart` overlays + re-locks on resume (`WidgetsBindingObserver`). Repository: `monthlyTrend()`, `merchantRanking()`, `paymentMethodTotals()`, `syncStatus()`. Tests: app_lock 7, reports 2, profile 2. Analyze clean. **Note:** 5.2 was previously just a dead wrapper (no persistence/gate) — now functional.
- **2026-08-08** — **Step 4.2 Transactions search/filter (feat/transactions-filter).** Search (merchant/note, case-insensitive) + filters (category, exact merchant, payment method, date preset: all/today/month/last-30/custom range), clear-filters, no-match empty state. Pure client-side `TransactionFilter` over the watched list — `lib/core/transaction_filter.dart`, unit-tested (10 tests). Also: fixed stale `transaction_repository_test.dart:32` (asserted Zomato uncategorized; seeded rules now map it → Food) — the test was written pre-categorization and never ran on Windows. **Windows test finding:** the sqlite3 native-assets blocker is *recoverable* on Flutter 3.44.9 — kill leftover `dart`/`flutter_tester` procs + `flutter clean`, then tests run (see `docs/troubleshooting.md`). Analyze clean; targeted suites pass (filter 10/10, repo 11/11). Full `flutter test` NOT run (flaky on Windows host). Next: 4.3 Reports, 4.5 Profile, on-device verify.
- **2026-08-07** — **Phase 4 core + Phase 5 code (feat/phase4).** Tabbed shell (Home/Transactions/Budget). 4.1 Home dashboard (today/month/budget-left + by-category bars). 4.4 Budget tab (monthly limits, 50/80/100 alerts, add/edit/delete; local-only writes, not synced — pending Supabase budgets schema). 5.1 Export CSV/JSON. 5.2 AppLock (local_auth wrapper; toggle deferred to 4.5 Profile). Tests: budget + export. **Deferred:** 4.2 search/filter, 4.3 Reports, 4.5 Profile, on-device verify, 5.3 Play assets, 5.4/5.5 release. Analyze clean.
- **2026-08-07** — **Step 3.1 + 3.2 merged (feat/supabase-sync).** Sync: dirty/remoteId on transactions (schema v2), SyncEngine push/pull, LWW by updated_at, 23505 conflict recovery, triggers on auth login + inbox drain + manual adds. Notifications: 9PM daily + Sunday weekly Hinglish summaries, channel + permission + device tz, re-scheduled each app open. **Known simplification:** summary push body is as-of-last-app-open, not fire-time — a background fill needs a foreground service/headless task (add when 9PM accuracy matters, `ponytail:` noted in `lib/data/notifications.dart`). `flutter analyze` clean; sync + notification tests pass; full `flutter test` still blocked on Windows host (sqlite3 native-assets, `docs/troubleshooting.md`). Next: on-device verify (2.3 + 3.1), then Phase 4. (Akash)
- **2026-08-06** — Repo created, docs written. No code. (Akash)
- **2026-08-06** — Rule added: every commit updates necessary md files (state/prd/design/plan/handoff/changelog). Fresh sessions reload everything from md. (Akash)
- **2026-08-06** — Google OAuth set up: Android client `com.kharcha.app` SHA1 `81:C9:...:44:AE`, Web client `kharcha-web` (ID `820021611320-...3sd`), Google provider enabled in Supabase. Pending: redirect URI `.../auth/v1/callback` in Web client's Authorized redirect URIs. Steps 1.1-1.3 done (scaffold, auth code, drift schema + seed + migration). Next: 2.1 manual entry. (Akash)
- **2026-08-06** — **Notification policy decision (owner):** pushes are value-only, never random. Hard cap **5/day max**. Targeted, user-opted-in messages only (9PM daily summary, Sunday weekly, budget alerts). Rationale: random/marketing push to a finance app violates Play policy and gets notification permission revoked → kills UPI auto-capture. Recorded in `docs/design.md` Principles. (Akash)
- **2026-08-06** — **Nudge style refined (owner):** the "value-only" pushes are Swiggy/Zomato-style **rule-based behavioral nudges** — deterministic local rules, zero AI. e.g. "Aaj kharcha add nahi kiya, bhai" (no expense by 9PM), "Swiggy ka order? Category add karo" (uncategorized). Always capped 5/day, user-opted-in, never random. Recorded `docs/design.md` scope item 10. (Akash)
- **2026-08-06** — **Transactions editable + notes** (owner): every transaction can be edited (amount, merchant, category, note, date, payment method) and carries an optional free-text note ("what I bought"), incl. auto-captured spends. Recorded `docs/design.md` scope 7b. (Akash)
- **2026-08-06** — **Windows test blocker:** local `flutter test` on Windows crashes on sqlite3 native-assets (docs/troubleshooting.md). CI was briefly added then **removed** (free-tier Actions unreliable) — test gate = local `flutter analyze` + `flutter test`. Code for 2.1–2.3 merged to main, analyze clean. (Akash)
- **2026-08-06** — **Session close.** Phase 2 (2.1 manual entry, 2.2 categorization, 2.3 UPI capture) merged to main (`c4a5935`). Docs updated (state/design/handoff/troubleshooting/CLAUDE.md). Next: 2.3 on-device verify, then Phase 3 sync. CI removed — do not re-add unless Actions reliability is confirmed. (Akash)
- **2026-08-08** — **Transactions edit/delete + tombstone sync (v0.2.1).** Edit reuses the add form (prefilled via `state.extra`); delete is confirm-gated. Deletes of pushed rows write a `deleted_transactions` tombstone (schema v8) — sync DELETEs the remote row on push and skips tombstoned ids on pull, so nothing resurrects. `updateTransaction` marks rows dirty → overwrite on push. Tests added (repo edit/delete/tombstone group). `flutter analyze` clean; `flutter test` still host-blocked. `docs/state.md`, `docs/changelog.md` updated. (Akash)
- **2026-08-23** — **v0.2.7 — non-transaction spam filter + SMS onboarding + full data layer audit fixes.** (1) **`_nonTransactionRe`** — a pre-compiled `RegExp` in `lib/core/upi_parser.dart` fires at step 0 of `parseUpiNotification()` before any amount extraction. Matches: recharge expiry/validity prompts ("plz recharge", "recharge ending", "validity expires"), bill due/overdue, OTP/verification codes, loan promos/pre-approved, payment collect-requests, failed/declined transactions. Returns `null` immediately — no false positives for real completed debit messages. 8 new test cases in `test/upi_parser_test.dart`. (2) **Onboarding SMS step** — `lib/screens/onboarding_screen.dart` Step 1 now shows a secondary optional `OutlinedButton` "Enable SMS capture (Optional)". `_stepDone` = `_capture || _sms` (either notification listener OR SMS read alone lets user proceed). `_PermissionStep` widget extended with `secondaryActionLabel`/`secondaryDone`/`onSecondaryAction` optional params. (3) **`MainActivity.kt` MethodChannel** — `requestSmsPermission` case: `ActivityCompat.requestPermissions` for both `RECEIVE_SMS` + `READ_SMS` (REQ_SMS = 3002). `getCaptureStatus` now returns 4 keys: `capture`, `notifications`, `battery`, `sms`. (4) **Data Layer Audit Fixes** — `transaction_repository.dart` rewritten to use Drift SQL aggregations (`sum`, `count`, `groupBy`) for merchantRanking, paymentMethodTotals, and syncStatus, preventing OOM crashes on large datasets. Importer now uses an internal category cache map to eliminate N+1 queries. Budget threshold spam fixed (only alerts on explicitly crossing 80/100%). Update checker rate-limited to 1 hit/hr via `_lastChecked` to avoid GitHub API 429 errors. (5) **187/187 tests**, `flutter analyze` clean. (6) **arm64 APK** — 28.3 MB, debug-signed, uploaded as `kharcha-armv8a-release.apk` on freshly recreated v0.2.7 release. Version `0.2.7+9`. ⚠ Signing key parked at `android/key.properties.release` — DO NOT restore while sideloading. (Akash)
