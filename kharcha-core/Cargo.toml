[package]
name = "kharcha_core"
version = "0.1.0"
edition = "2021"
description = "Kharcha core logic: money, categorization, UPI/bank SMS parsing. No Dart, no Supabase."

[lib]
crate-type = ["cdylib", "rlib"]


[dependencies]
fancy-regex = "0.14"
regex = "1"
uniffi = { version = "0.32.1", features = ["cli"] }

[dev-dependencies]
